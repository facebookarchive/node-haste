/**
 * @provides javelin-tag
 * @requires javelin-dom
 *           javelin-install
 *           javelin-stratcom
 * @javelin
 */

JX.install('MSteps', {
  foo: function() {
    return new JX.URL();
  }
});
